# comment gère un projet en cli


### lister les projets
```cmd
gcloud projects list
```

### Crée un projet
```cmd
gcloud projects create PROJECT_ID --name="Nom du projet"
```

### Delete un project
```cmd
gcloud projects delete PROJECT_ID
```

### switch de project

```cmd
gcloud config set project tp3-infracloud-m1
```